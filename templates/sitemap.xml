<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <!-- Homepage -->
    <url>
        <loc>{{ scheme }}://{{ domain }}/</loc>
        <changefreq>daily</changefreq>
        <priority>1.0</priority>
    </url>

    <!-- Static Pages -->
    <url>
        <loc>{{ scheme }}://{{ domain }}/products/</loc>
        <changefreq>daily</changefreq>
        <priority>0.9</priority>
    </url>

    <url>
        <loc>{{ scheme }}://{{ domain }}/offers/</loc>
        <changefreq>daily</changefreq>
        <priority>0.8</priority>
    </url>

    <!-- Categories -->
    {% for category in categories %}
    <url>
        <loc>{{ scheme }}://{{ domain }}{{ category.get_absolute_url }}</loc>
        <changefreq>weekly</changefreq>
        <priority>0.8</priority>
        <lastmod>{{ category.updated_at|date:"Y-m-d" }}</lastmod>
    </url>
    {% endfor %}

    <!-- Products -->
    {% for product in products %}
    <url>
        <loc>{{ scheme }}://{{ domain }}{{ product.get_absolute_url }}</loc>
        <changefreq>weekly</changefreq>
        <priority>0.6</priority>
        <lastmod>{{ product.updated_at|date:"Y-m-d" }}</lastmod>
    </url>
    {% endfor %}
</urlset>
